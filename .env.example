# GameDevBoard Environment Configuration
# Copy this file to .env and fill in the required values

# =============================================================================
# REQUIRED ENVIRONMENT VARIABLES (Server will not start without these)
# =============================================================================

# Database Configuration (CRITICAL)
# PostgreSQL connection string for the main database
# Format: postgresql://username:password@host:port/database_name
DATABASE_URL=postgresql://user:password@localhost:5432/gamedevboard

# Authentication & Security (CRITICAL)
# JWT secret key for token signing - MUST be at least 32 characters
# Generate with: openssl rand -base64 32
JWT_SECRET=your-jwt-secret-key-must-be-at-least-32-characters-long

# AI Integration (CRITICAL)
# OpenAI API key for AI-powered game development features
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# =============================================================================
# OPTIONAL ENVIRONMENT VARIABLES (Have defaults or fallbacks)
# =============================================================================

# Server Configuration
# Port for the server to listen on (default: 5000)
PORT=5000

# Environment mode: development, production, or test (default: development)
NODE_ENV=development

# Alternative AI Integration (Optional)
# Alternative OpenAI configuration for advanced setups
# AI_INTEGRATIONS_OPENAI_API_KEY=sk-alternative-openai-key
# AI_INTEGRATIONS_OPENAI_BASE_URL=https://api.openai.com/v1

# Object Storage Configuration (Optional)
# For file uploads and asset management
# PUBLIC_OBJECT_SEARCH_PATHS=/path/to/public/assets
# PRIVATE_OBJECT_DIR=/path/to/private/storage

# Legacy Replit Variables (Optional - for migration compatibility)
# REPL_ID=your-repl-id-if-migrating-from-replit

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================
#
# 1. Copy this file: cp .env.example .env
# 2. Fill in the REQUIRED variables above
# 3. Optionally customize the OPTIONAL variables
# 4. Never commit your .env file to version control
#
# For production deployment:
# - Use strong, unique values for all secrets
# - Enable SSL/TLS for database connections
# - Use environment-specific configuration management
# - Regularly rotate API keys and secrets
#
# =============================================================================
